import { connect } from "react-redux";
import TestAdmin from "./TestAmin";

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(TestAdmin);
